import { Purchase } from '../sections';

const purchase = () => (
  <div className="purchase-page bg-primary-black overflow-hidden" style={{ width: '100%' }}>
    <div className="relative">
      <div className="gradient-03 z-0" />
      <Purchase />
    </div>
  </div>
);

export default purchase;
